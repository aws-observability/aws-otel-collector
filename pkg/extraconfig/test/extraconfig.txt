config=--config /opt/aws/aws-otel-collector/etc/config.yaml

# set debug level
loggingLevel=DEBUG	loggingLevel=DEBUG

# set AWS_PROFILE env var
AWS_PROFILE=test

# set AWS_CREDENTIAL_PROFILES_FILE
AWS_CREDENTIAL_PROFILES_FILE=~/.aws/credentials
